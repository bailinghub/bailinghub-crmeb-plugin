# Changelog

格式遵循语义化版本。适配器版本、工具能力集合版本与配置结构版本分别管理：只有对应契约真实变化时才递增。

## Unreleased

## 2.4.1 - 2026-08-10

### Fixed
- 后台增强界面不再把完整脚本写进 CRMEB 的 `system_config.value`；`custom_admin_js` 只保留小型同源加载器，完整界面由无扩展名动态入口 `/bailing/admin-bundle` 提供，同时避免字段长度限制和常见 Nginx `.js` 静态规则导致版本卡片与交互缺失
- `接入方 token` 与 `工具源签名密钥` 改由专用安全接口写入 `runtime/bailinghub-secrets/`：目录 `0700`、文件 `0600`、`flock` 串行化并使用同目录临时文件原子替换；读取只返回“是否已配置”
- 不再创建、读取或写入 `system_config` 中的秘密行；启动时精确删除旧 `bailing_access_token` / `bailing_sign_secret` 且不迁移，同时清除按名缓存；清理失败会阻止请求进入通用配置 API
- 手工安装/桥接会原子刷新插件生成的 `config/bailing.php`，避免旧文件在 boot 清理前继续把历史秘密载入框架配置内存
- 升级包完整性与切换后健康检查新增后台资源控制器、安全配置控制器、settings 运行文件及 `admin-bundle`、`settings/status`、`settings/save` 路由要求，拒绝缺件包

### Changed
- 配置引导明确区分三种来源：自建开源版、自建商业版的具体租户、以及没有自有部署时使用的在线体验租户；在线体验环境不是“商业版”的同义词
- 三种来源统一推荐从对应实例或具体租户的控制台复制完整聊天入口嵌入代码；“中枢地址 + entry key”只作为高级替代方式并要求成对填写
- 接入方 token 与工具源签名密钥分别保护管理员身份/聊天链路和工具源拉取/工具调用链路，不再被描述为同一组选填项
- 移除从未被运行时使用的路由 key；旧安装中的历史配置行会被清理
- 适配器版本保持 `2.4.1`；37 个工具契约未改变，能力集合版本保持 `2.3.0`；因秘密持久化结构真实改变，配置结构版本升级为 `2`
- 升级过程原地保留私密存储且不把它复制进升级备份或发布包；卸载只精确删除插件私密目录
- 手工安装或首次桥接后要求重载站点实际使用的 PHP-FPM（或重启 PHP 容器），再重新登录并强制刷新后台

## 2.4.0 - 2026-08-10

### Added
- “百灵中枢配置”页显示适配器版本、工具清单版本和配置结构版本
- 新增超级管理员本地升级入口：上传升级包后先展示候选版本、更新说明和兼容性检查，再二次确认
- 升级包引入独立 `plugin.json`、精确文件 SHA-256 与 RSA-SHA256 发布签名；服务端拒绝路径穿越、符号链接、额外文件和版本回退
- 升级过程使用并发锁、非 Web 暂存区、插件范围备份、完整目录切换和失败自动恢复，并记录不含凭据的升级历史
- 安装脚本先构建完整运行副本再切换，复制失败不会先删除旧版

### Fixed
- 规范化聊天组件的中枢地址：可直接粘贴控制台页面或 `widget.js` URL，并保留托管环境的 `/tenant/<tenantId>` 挂载前缀
- 体验环境根地址不再被误当作租户中枢地址；安装器会明确拒绝并提示从租户控制台复制地址
- 工具源 spec 不再匿名公开：`/bailing/tools.json` 固定要求与工具调用相同的 HMAC 签名，未签名和错误签名均返回 401
- 网页安装器新增服务器侧一次性安装口令：未授权请求不能触发解压、复制或配置写入，成功安装后自动销毁口令并以锁文件防重放

### Changed
- 后台配置说明、安装器和 README 区分自托管根地址与托管租户地址；`widget.js` 加载失败时输出可执行的排查提示
- 新增“聊天入口嵌入代码（推荐）”：可粘贴完整 `<script>` 自动解析 `src`/`data-entry`，底层继续保存中枢地址和公开 entry key
- 聊天入口 key 不再作为秘密遮罩；保留高级分开填写，裸 key 仅在已有中枢地址时可用
- 配置页新增显式、最长 5 秒的浏览器端连通验证；服务 boot 只做本地校验，不发阻塞外网请求
- 工具源登记文档固定要求“签名保护（signed_required）”，同一密钥覆盖 spec、工具调用与授权探针
- 嵌入代码解析后立即清空原始 HTML，并拒绝携带临时 `data-ticket` 的运行时代码，避免票据进入持久配置
- 适配器版本升级为 `2.4.0`；本次未改变 37 个工具契约，能力集合版本继续为 `2.3.0`
- 网页安装器明确只承担首次安装；后续升级统一进入登录后的 CRMEB 配置页

## 2.3.0 - 2026-08-06

### Fixed
- 修复工具回调只校验“CRMEB 管理员存在”便直接操作数据库、普通管理员可能绕过原生角色权限的问题
- 工具执行入口改为强制经过 `Verify::gate()`，验签与 CRMEB 原生权限裁决缺一不可，授权失败返回 HTTP 403

### Changed
- 新增 CRMEB 原生权限适配器：37 个工具逐一映射到等价的后台菜单、按钮或 API 权限点
- 每次执行实时读取 `system_admin`、启用中的 `system_role.rules` 和 `system_menus`；不在票据、中枢或插件中保存角色与权限快照
- 保持演示插件零中枢审批：CRMEB 有权限即直接执行，CRMEB 无权限即拒绝，最终权限唯一真值归业务侧
- 能力集合版本升级为 `2.3.0`

## 2.2.0 - 2026-08-06

### Changed
- 演示插件的 14 个后台写能力统一采用直接执行模型：保留 `low`/`medium` 风险分级与审计，不再声明中枢人工审批
- 移除商品改价、订单发货/确认收货、售后同意/拒绝、用户启禁用、定向发券、拒绝提现共 8 处 `approval.required`
- `extract_refuse` 从 `high` 调整为 `medium`；仍保留敏感参数审计脱敏、登录主体要求和 CRMEB 端账号校验
- 能力集合版本升级为 `2.2.0`，便于中枢识别并重新拉取声明

## 2.1.0 - 2026-08-04

### Changed（架构重构，向后兼容）
- 声明层模块化拆分：单一 `BailingSpec.php` 拆分为 `src/spec/` 下 8 个领域声明模块（Product/Order/Refund/User/Marketing/Finance/Stats/Store），每个模块自管 path 映射与能力声明，新增/修改能力只动对应模块文件
- `BailingSpec` 变为注册中心 + `SPEC_VERSION` 常量，版本号输出至 `tools.json` 的 `info.version`
- 明确架构原则：元能力（原子能力）声明、零侵入安装、版本化升级，写入 README

### Fixed
- 修复 `config/bailing.php` 在 TP 配置加载期调用 `sys_config()` 导致的全站 500（helper 未注册时 fatal）
- 修复控制器从 `Config` 读取签名密钥永远为空导致的工具调用 401（改为请求期直读 `sys_config`）

## 2.0.0 - 2026-08-04

### Added
- 工具集从 3 个系统性扩展至 37 个原子能力，覆盖 CRMEB 后台 8 大业务域（商品/订单/售后/用户/营销/财务/统计/门店）
- 商品：详情、分类树、上下架、改库存、改价、评论查/审/回
- 订单：详情（含商品明细）、状态统计、发货、确认收货、备注
- 售后：列表、同意、拒绝（状态流转，资金打款走原支付通道人工执行）
- 用户：列表、详情、启禁用、会员等级
- 营销：优惠券查询/定向发放、秒杀/拼团/砍价活动查询
- 财务：提现列表、拒绝提现（退回余额+流水）、资金流水
- 统计：经营总览、营业额趋势、销量排行
- 门店：分销员、门店、物流公司

### Changed
- `product_create` 增强主图、单位、运费模板自动匹配与分类有效性校验

## 1.0.0 - 2026-08-03

### Added
- 首个可用版本：tools.json 工具源端点、authz-probe 授权探针、签名验签、后台配置页、聊天组件注入
- 初始 3 个工具：product_list / order_list / product_create

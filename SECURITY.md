# 安全说明

## 支持范围

我们优先为最新正式版本提供安全修复。旧版本发现问题后，建议先升级到最新版本再复现；只有仍受影响的版本才会继续进入修复评估。

| 版本 | 安全支持 |
| --- | --- |
| 最新正式版本 | 支持 |
| 更早版本 | 建议先升级 |
| 修改过的第三方副本 | 由修改方自行维护 |

## 报告安全问题

请不要在公开 Issue、Discussion、截图或日志中披露尚未修复的漏洞、访问令牌、签名密钥、Cookie、管理员身份信息或真实业务数据。

推荐使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口：

<https://github.com/bailinghub/bailinghub-crmeb-plugin/security/advisories/new>

报告中请尽量提供：

- 插件版本、CRMEB 版本和 PHP 版本；
- 安装方式以及是否修改过插件代码；
- 可重复的最小步骤和实际影响；
- 已脱敏的请求、响应或日志；
- 如已知，可行的临时缓解方法。

我们会先确认问题是否属于本插件边界，再协调验证、修复和披露时间。修复发布前，请给维护者保留合理的处理窗口。

## 安全边界

- 插件复用 CRMEB 当前管理员、角色和原生权限，不会绕过 CRMEB 的授权边界；错误的 CRMEB 权限配置不等同于插件漏洞。
- 接入方 token 与工具源签名密钥属于服务端秘密，不应提交到 Git、写进公开 Issue 或发送给非维护人员。
- 只有从本仓库正式 Release 下载并通过随包校验信息验证的安装包，才属于维护者发布的制品。
- 部署前应备份站点，并先在测试环境验证；正式站点的第三方扩展、反向代理和自定义改动可能改变实际安全边界。

